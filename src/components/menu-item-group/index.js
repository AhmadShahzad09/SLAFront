import OdsMenuItemGroup from '../menu/src/menu-item-group';

/* istanbul ignore next */
OdsMenuItemGroup.install = function(Vue) {
  Vue.component(OdsMenuItemGroup.name, OdsMenuItemGroup);
};

export default OdsMenuItemGroup;
